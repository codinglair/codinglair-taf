# Codinglair TAF 1.2.0 release-candidate disposition

## Candidate identity

| Field | Value |
| --- | --- |
| Version | `1.2.0` |
| Candidate commit/tag | Pending reviewed release commit/tag |
| Candidate build timestamp | Pending exact-candidate workflow |
| Maven coordinates | `com.codinglair.taf:*:1.2.0` |
| Immutable image | `codinglair/codinglair-taf-mcp:1.2.0@sha256:<pending>` |
| Optional `latest` alias | Pending; if published, it must resolve to the same digest |
| Community contents | Runtime, starters, consumer BOM, and MCP artifacts |
| Excluded contents | Proprietary Quality Intelligence and demo artifacts |

## Release checklist and evidence

This record is fail-closed. `PENDING` and `NOT RUN` are not passes. Final evidence must identify the
exact candidate SHA/tag, workflow run, environment, versions/digests, command, result, retained
artifact, limitation, and disposition.

| Gate | Required evidence | Preparation disposition |
| --- | --- | --- |
| Gate A–D assignments and reviews | Handoffs and review reports for PKG/SCF/MCP/SEC/INF/VER assignments | PASS for repository completion records; exact-candidate rerun pending |
| Version, BOM, coordinates, POM metadata | Root revision, staged release contract, effective POMs | Prepared at `1.2.0`; exact staging result pending |
| Sources, Javadocs, SBOMs, checksums, licenses | Staged repository contract and license-policy output | PENDING exact candidate |
| External starter and direct-module consumers | Maven Invoker reports resolving staged artifacts only | Prior Gate D PASS; PENDING exact 1.2.0 candidate rerun |
| Generated Web + API + Database project | Deterministic generation and outside-reactor build report | Prior Gate D PASS; PENDING exact candidate rerun |
| Image profiles and Kind | STDIO/HTTP/Keycloak/Kind qualification artifact | Prior Gate D PASS; PENDING exact published digest rerun |
| Docker Hub digest and optional alias | `digest.txt`, manifest inspection, Cosign verification | PENDING authorized publication workflow |
| Image SBOM/provenance/scan | Digest-bound SPDX, attestations, Trivy report/version, Critical-CVE disposition | PENDING authorized publication workflow |
| Clean public-repository clone | Credential-free checkout, clean status, exact SHA, release workflow result | Automated in `release.yml`; PENDING exact candidate run |
| Upload-without-publish Central bundle | Ephemeral signature dry run, bundle inventory, signature coverage | Automated with `central.skipPublishing=true`; PENDING exact candidate run |
| Documentation and compatibility | Docs profile, snippet manifest, changelog, notes, migration/deprecation review | Prepared; exact-candidate docs result pending |

## BLD-120-001 Javadoc publication-size correction

The earlier Maven Central attempt is **BLOCKED/FAILED** because Java 25 font resources duplicated
across module Javadoc JARs exceeded the publication-size limit. It is not a successful Central
deployment or validation result.

The candidate correction upgrades the centrally managed Maven Javadoc Plugin from `3.11.1` to
`3.11.3` and explicitly retains the plugin's default Java 23+ no-fonts behavior with
`<disableNoFonts>false</disableNoFonts>`. Release-profile effective POMs for `taf-api-rest`,
`codinglair-taf-runtime-core`, `taf-messaging-aws`, and `taf-mcp-tools` contain version `3.11.3`,
the no-fonts setting, and the `release-javadocs` attachment.

Local Java 25 release-staging evidence from the uncommitted working tree based on
`30b7a4e94adff8e5711d2e61d0678968e77a2a6f`:

| Measurement | Result |
| --- | ---: |
| Generated reactor Javadoc JARs | 38 |
| Aggregate generated Javadoc size | 8,044,316 bytes |
| Staged/published Javadoc JARs | 37 |
| Aggregate staged Javadoc size | 7,826,498 bytes |
| Total local staging-repository footprint | 17,914,387 bytes |
| Javadoc JARs containing `resource-files/fonts/` | 0 |
| Staged source JARs | 37 |
| Staged SHA-1 / MD5 checksum files | 274 / 274 |

| Representative module | Compressed | Uncompressed | Entries | Fonts | `index.html` |
| --- | ---: | ---: | ---: | ---: | ---: |
| `taf-api-rest` | 169,602 | 674,715 | 73 | 0 | present |
| `codinglair-taf-runtime-core` | 777,560 | 4,247,290 | 315 | 0 | present |
| `taf-messaging-aws` | 413,936 | 2,094,242 | 165 | 0 | present |
| `taf-mcp-tools` | 449,532 | 2,438,640 | 161 | 0 | present |

The `taf-api-rest` result is consistent with the 166 KB proof of concept and is approximately 96%
smaller than the ~4.02 MB failed-attempt baseline. The clean 51-project
`.\mvnw.cmd clean deploy -Prelease-staging` build passed in 3:52, and the staged release contract
and license policy passed. Maven Central upload/validation, the signed Central no-upload bundle,
merge to `master`, and recording of the accepted authoritative SHA remain pending release-owner
actions. No publication was performed by BLD-120-001.

Publication-hygiene correction: `playwright-sauce-demo` was present in an earlier Central release
because its deploy skip applied only to `release-staging`, while the Central extension assembled
its own reactor bundle. The Central Publishing plugin now uses its supported
`<excludeArtifacts>playwright-sauce-demo</excludeArtifacts>` control. The staged-repository contract
and Central-bundle workflow check both reject `com/codinglair/taf/demo/`. Previously published
immutable Central coordinates cannot be removed or overwritten; this correction prevents the demo
from being included in subsequent release bundles.

## Known limitations and required disposition

- Maven is the supported blueprint output. Gradle guidance covers dependency declarations only.
- Kubernetes is supported for Streamable HTTP as a single-replica, process-local reference
  topology. Pod replacement does not preserve jobs, approvals, audit history, or results.
- Android/UiAutomator2 is supported; iOS/XCUITest generation is not.
- `latest` is optional and prohibited in reproducible consumers. If published, evidence must show
  that it equals the immutable `1.2.0` manifest digest.
- No resolved secret, credential, authorization value, receipt handle, raw sensitive payload, or
  proprietary artifact may appear in retained evidence.

## Rollback

Before promotion, delete or expire failed staging/evidence artifacts and rebuild from a corrected,
reviewed commit. After Maven publication, never overwrite released coordinates; publish a patch
release with migration or security guidance. For an image issue, remove the mutable alias from
promotion paths, retain the immutable digest and provenance for investigation, and publish a new
immutable patch tag. Revoke or mark compromised signatures/attestations according to the incident
process.

## Release decision

**Current decision: NOT APPROVED.** Promotion remains blocked until every mandatory exact-candidate
gate above is `PASS`, review findings are closed or explicitly accepted, and an authorized human
records `APPROVE 1.2.0`, `APPROVE 1.2.0 WITH EXCEPTION`, or `REJECT 1.2.0` with approver, timestamp,
candidate SHA/tag, evidence links, and any exception owner/expiry.
