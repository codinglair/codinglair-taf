package com.codinglair.taf.runtime.file;

/** Structured and binary file formats supported by the validation controller. */
public enum FileFormat {
  JSON,
  XML,
  CSV,
  EXCEL,
  PDF,
  FIXED_WIDTH
}
