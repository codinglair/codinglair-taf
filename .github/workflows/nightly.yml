name: Nightly Verification

on:
  schedule:
    - cron: '17 6 * * *'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: nightly-verification
  cancel-in-progress: false

jobs:
  complete-matrix:
    name: Complete scheduled matrix
    uses: ./.github/workflows/verification-matrix.yml
